<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bulmeong.basecamp.club.mapper.ClubSqlMapper">

    <!-- 소모임 생성 -->
    <insert id="insertClubDto">

        <selectKey resultType="int" keyProperty="id" order="AFTER">
            select max(id) from bc_club
        </selectKey>

        insert into bc_club(
            user_id, 
            category_id,
            region_id, 
            name, 
            description, 
            capacity, 
            main_image
        )values(
            #{user_id}, 
            #{category_id},
            #{region_id}, 
            #{name}, 
            #{description}, 
            #{capacity}, 
            #{main_image}
        )
    </insert>


    <!-- 소모임 가입조건 -->
    <insert id="insertClubJoinCondition">
        insert into bc_club_join_condition(
            club_id, start_year, end_year, gender
        )values(
            #{club_id},#{start_year},#{end_year},#{gender}
        )
    </insert>

    <!-- 게시글 작성 -->
    <insert id="insertClubPostDto">

        <selectKey resultType="int" keyProperty="id" order="AFTER">
            select max(id) from bc_club_post
        </selectKey>
        
        insert into bc_club_post(
	        club_id, 
            user_id, 
            category_id, 
            title, 
            content, 
            read_count
        )values(
	        #{club_id},
            #{user_id}, 
            #{category_id},
            #{title}, 
            #{content}, 
            0
        )
    </insert>

    <!-- 게시글 사진 첨부 -->
    <insert id="insertClubPostImage">
        insert into bc_club_post_image (
	        post_id, post_img_location
        )values(
            #{post_id},#{post_img_location} 
        )
    </insert>


    <!-- 소모임 회원 가입 -->
    <insert id="insertClubMemberDto">
        insert into bc_club_member(
	        club_id, 
            user_id, 
            role_id
        )values(
	        #{club_id}, 
            #{user_id}, 
            #{role_id} 
        )
    </insert>
    
    <!-- 관심사 카테고리 -->
    <select id="selectClubCategory" resultType="com.bulmeong.basecamp.club.dto.ClubCategoryDto">
        select * from bc_club_category
    </select>


    <!-- 지역 카테고리 -->
    <select id="selectRegionCategory" resultType="com.bulmeong.basecamp.club.dto.ClubRegionCategoryDto">
        select * from bc_club_region_category
    </select>

    <!-- 게시글 각각의 카테고리 (by id) -->
    <select id="selectPostCategoryDtoById" resultType="com.bulmeong.basecamp.club.dto.ClubPostCategoryDto">
        select * from bc_club_post_category
        where id = #{id}
    </select>

    <!-- 게시글 카테고리 -->
    <select id="selectPostCategoryDto" resultType="com.bulmeong.basecamp.club.dto.ClubPostCategoryDto">
    select * from bc_club_post_category
    </select>

    <select id="selectPostCategoryName" resultType="String">
        select category_name from bc_club_post_category
        where id = #{id}
    </select>

    <!-- 소모임 목록 뽑기 -->
    <select id="selectClubList" resultType="com.bulmeong.basecamp.club.dto.ClubDto">
        select * from bc_club
        order by id desc
    </select>

    <!-- 가입한 소모임 목록 뽑기 (by user_id) -->
    <select id="selectJoinClubList" resultType="com.bulmeong.basecamp.club.dto.ClubMemberDto">
        select * from bc_club_member
        where user_id = #{user_id}
    </select>

    <!-- 각 소모임의 게시판 글 목록 뽑기 -->
    <select id="selectClubPostDtoList" resultType="com.bulmeong.basecamp.club.dto.ClubPostDto">
        select * from bc_club_post
        where club_id = #{club_id}
        order by id desc;
    </select>

    <select id="selectUserDtoById" resultType="com.bulmeong.basecamp.user.dto.UserDto">
        select * from bc_user 
        where id = #{id}
    </select>

    <select id="selectClubMemberDto" resultType="com.bulmeong.basecamp.club.dto.ClubMemberDto">
        select * from bc_club_member
        where club_id = #{club_id}
        and user_id = #{user_id}
    </select>



    <!-- 이미지dto 리스트 뽑기 -->
    <select id="selectPostImageDtoList" resultType="com.bulmeong.basecamp.club.dto.ClubPostImageDto">
        select * from bc_club_post_image;
    </select>

    <!-- selectPostImageDto by id -->

    <select id="selectPostImageDtoListById" resultType="com.bulmeong.basecamp.club.dto.ClubPostImageDto" >
        select * from bc_club_post_image
        where post_id = #{post_id}
    </select>

    <!-- 게시글DTO 가져오기 -->
    <select id="selectPostDtoById" resultType="com.bulmeong.basecamp.club.dto.ClubPostDto">
        select * from bc_club_post
        where id = #{id}
    </select>

    <!-- 모든 게시글 가져오기 + 조회수 순서로 정렬 -->
        <select id="selectAllPosts" resultType="com.bulmeong.basecamp.club.dto.ClubPostDto">
            select * from bc_club_post
            order by read_count desc;
        </select>

    <!-- 게시글 댓글 인서트 -->
    <insert id="insertClubPostCommentDto">
        insert into bc_club_post_comment(
	        post_id, user_id, content
        )values(
            #{post_id}, #{user_id}, #{content}
        )
    </insert>

    <!-- 게시글 댓글 삭제  -->
    <!-- <delete id ="deleteComment">
        delete from bc_club_post_comment 
        where id = #{id}
    </delete> -->

    <!-- 게시글 댓글 수정  -->
    <!-- <updete id="updateComment">
        update bc_club_post_comment 
        set content = #{content}
        where id = #{id}
    </updete> -->




    <!-- 게시글 댓글 DTO 가져오기 by post_id -->
    <select id="selectPostCommentDto" resultType="com.bulmeong.basecamp.club.dto.ClubPostCommentDto">
        select * from bc_club_post_comment
        where post_id = #{post_id}
    </select>

    <!-- 하나의 소모임 DTO 가져오기 by id -->
    <select id="selectClubDtoById" resultType="com.bulmeong.basecamp.club.dto.ClubDto">
        select * from bc_club
        where id = #{id}
    </select>

    <!-- 하나의 소모임 카테고리 가져오기 -->
    <select id="findClubCategoryNameById" resultType="String">
        select category_name from bc_club_category
        where id = #{id}
    </select>
    

    <!-- 지역카테고리 DTO 가져오기 by id-->
    <select id="selectRegionCategoryDtoById" resultType="com.bulmeong.basecamp.club.dto.ClubRegionCategoryDto">
        select * from bc_club_region_category
        where id=#{id}
    </select>



    <!-- 소모임 북마크 유무 확인 select  -->
    <select id="confirmBookmark" resultType="int">
        select count(*) from bc_club_bookmark
        where club_id = #{club_id}
        and user_id = #{user_id}
    </select>
    

    <!-- 소모임 북마크 유 delete -->
    <delete id="deleteBookmark">
        delete from bc_club_bookmark
        where club_id=#{club_id} 
        and user_id=#{user_id}
    </delete>

    <!-- 소모임 북마크 무 insert -->
    <insert id="insertBookmark">
        insert into bc_club_bookmark(
            club_id, user_id
        )values(
            #{club_id}, #{user_id}
        ); 
    </insert>

    <!-- 소모임 북마크 집계 -->
    <select id="countTotalBookmark" resultType="int">
        select count(*) from bc_club_bookmark
        where club_id = #{club_id}
    </select>



    <!-- 소모임 게시글 조회수 -->
    <select id="increaseReadCount" resultType="int">
        update bc_club_post
        set read_count = read_count + 1
        where id = #{id}
    </select>

    <!-- 게시글 조회수 집계 -->
    <select id="totalReadCount" resultType="int">
        select read_count 
        from bc_club_post where id=#{id}
    </select>
    


    <!-- 소모임 게시글당 스크랩 개수 집계  select count -->
    <select id="countTotalPostLike" resultType="int">
       select count(*) from bc_club_post_like
       where post_id = #{post_id}
    </select>

    <!-- 소모임 게시글 스크랩 유무 확인 selct count -->
    <select id="confirmPostLike" resultType="int">
        select count(*) from bc_club_post_like
        where post_id = #{post_id}
        and user_id = #{user_id}
    </select>

    <!-- 소모임 게시글 스크랩 유 delete -->
    <delete id="deletePostLike">
        delete from bc_club_post_like
        where post_id = #{post_id}
        and user_id = #{user_id}
    </delete>


    <!-- 소모임 게시글 스크랩 무 insert -->
    <insert id="insertPostLike">
        insert into bc_club_post_like(
	        post_id, user_id
        )values(
	        #{post_id},#{user_id}
        )
    </insert>


    <!-- 댓글 좋아요 유무 확인 select count -->
    <select id="confirmCommentLike" resultType="int">
        select count (*) from bc_club_post_comment_like
        where comment_id = #{comment_id}    
        and user_id = #{user_id}
    </select>


    <!-- 댓글 좋아요 insert -->
    <insert id="insertCommentLike">
        insert into bc_club_post_comment_like(
	        comment_id, user_id
        )values(
	        #{comment_id}, #{user_id}
        )
    </insert>


    <!-- 댓글 좋아요 delete -->
    <delete id="deleteCommentLike">
        delete from  bc_club_post_comment_like
        where comment_id = #{comment_id}
        and user_id = #{user_id}
    </delete>

    <!-- 대댓글 인서트 -->
    <insert id="insertNestedCommentDto">
    insert into bc_club_nested_comment(
	    comment_id, user_id, content
    )values(
	    #{comment_id}, #{user_id}, #{content}
    )
    </insert>

    <!-- 대댓글DTO 가져오기 by comment_id -->
    <select id="selectNestedCommentDtoList" resultType="com.bulmeong.basecamp.club.dto.ClubNestedCommentDto">
        select * from bc_club_nested_comment
        where comment_id  = #{comment_id}
    </select>



    <!-- 소모임 댓글 총개수 집계 -->
    <select id="countTotalComment" resultType="int">
    select COUNT(*) from bc_club_post_comment
    where post_id = #{post_id}
    </select>

    <!-- 특정 소모임에 가입된 회원 뽑아오기 -->
    <select id="selectClubMemberDtoList" resultType="com.bulmeong.basecamp.club.dto.ClubMemberDto">
        select * from bc_club_member
        where club_id = #{club_id}
        order by id desc
    </select>

    <!-- 특정 소모임에 가입한 회원수 집계 -->
    <select id="countTotalClubMember" resultType="int">
        select count(*) from bc_club_member
        where club_id = #{club_id}
    </select>

    <!-- 소모임 가입 여부 확인  -->
    <select id="checkClubMembership" resultType="int">        
        select count(*) from bc_club_member
        where club_id = #{club_id}
        and user_id = #{user_id}
    </select>


    <!-- 특정 클럽의 사진만 뽑아오기 -->
    <select id="selectPostImageDtoByPostId" resultType="com.bulmeong.basecamp.club.dto.ClubPostImageDto">
        select * from bc_club_post_image
        where post_id in (
	        select id from bc_club_post
	        where club_id = #{club_id}
        )
    </select>

    <!-- 특정 게시글의 사진 리스트 뽑기 하나으 글에 들어가있는 사진을 뽑아오는 쿼리 위에 쿼리 수정필요-->
    <select id="selectPostImageList" resultType="com.bulmeong.basecamp.club.dto.ClubPostImageDto">
        SELECT * FROM bc_club_post_image bcpi
        where post_id = #{post_id}
    </select>

    <!-- 북마크한 소모임 뽑아오기 -->
    <select id="selectBookmarkedClubDtoList" resultType="com.bulmeong.basecamp.club.dto.ClubDto">
        select * from bc_club
        where id in (
	        select club_id from bc_club_bookmark
	        where user_id = #{user_id}
        )

    </select>

    <!-- 정모 개설하기 -->
    <insert id="insertClubMeetingDto">

    <selectKey resultType="int" keyProperty="id" order="AFTER">
            select max(id) from bc_club_meeting
    </selectKey>

        insert into bc_club_meeting(
            club_id,
            user_id,
            main_image,
            name,
            description,
            location,
            meeting_date,
            fee,
            capacity
         )values(
	        #{club_id}, 
            #{user_id}, 
            #{main_image}, 
            #{name}, 
            #{description}, 
            #{location}, 
            #{meeting_date}, 
            #{fee}, 
            #{capacity}
        )
    </insert>

    <!-- 정모 회원 등록하기 -->
    <insert id="insertClubMeetingMemberDto">
        insert into bc_club_meeting_member(
	        meeting_id, user_id
        )values(
	        #{meeting_id}, #{user_id}
        )
    </insert>

    <!-- 정모 카드 출력하기 -->
    <select id="selectClubMeetingDtoList" resultType="com.bulmeong.basecamp.club.dto.ClubMeetingDto">
        select * from bc_club_meeting
        where club_id = #{club_id}
        order by meeting_date
    </select>

    <!-- 정모 지원 인원 -->
    <select id="countTotalMeetingMember" resultType="int">
        select count(*)from bc_club_meeting_member
        where meeting_id = #{meeting_id}
    </select>

    <!-- 정모 개수 카운트 -->
    <select id="countTotalMeeting" resultType="int">
        select count(*) from bc_club_meeting
        where club_id = #{club_id}     
    </select>

    <!-- 소모임 정원 카운트 -->
    <select id="confirmCapacity" resultType="int">
        select capacity from bc_club
        where id = #{id}
    </select>

    <!-- 소모임 가입조건 확인 -->
    <select id="selectClubJoinCondition" resultType="com.bulmeong.basecamp.club.dto.ClubJoinConditionDto">
        SELECT * FROM bc_club_join_condition
        WHERE club_id = #{club_id}
    </select>

    <!-- 정모 가입여부 확인 -->
    <select id="selectMeetingMember" resultType="com.bulmeong.basecamp.club.dto.ClubMeetingMemberDto">
        select * from bc_club_meeting_member
        where meeting_id  = #{meeting_id}
        and user_id = #{user_id}
    </select>

    <!-- 정모 탈퇴 -->
    <delete id = "deleteMeetingMember">
        delete from bc_club_meeting_member
        where meeting_id = #{meeting_id}
        and user_id = #{user_id}
    </delete>

    <!-- 소모임 어제의 게시글 수 집계 -->
    <select id="yesterdayPostCount" resultType="int">
        <![CDATA[
        select count(*) 
        from bc_club_post
        where club_id = #{club_id}
        and created_at >= curdate() - interval 1 DAY
        and created_at < curdate()
        ]]>
    </select>

    <!-- 소모임 어제의 가입자 수 집계 -->
     <select id="yesterdayNewMembers" resultType="int">
        <![CDATA[
        select count(*)
        from bc_club_member
        where club_id = #{club_id}
        and joined_at >= curdate() - interval 1 DAY 
        and joined_at < curdate()
        ]]>
    </select>

    <!-- 방문자수 증가 -->
    <insert id="increaseVisitCount">
        insert into bc_club_visit (
	        club_id, user_id 
        )values(
	        #{club_id}, #{user_id}
        )
    </insert>

    <!-- 방문기록 조회 -->
    <select id="selectTodayVisit" resultType="com.bulmeong.basecamp.club.dto.ClubVisitDto">
    SELECT * FROM bc_club_visit
	where club_id = #{club_id}
	and user_id = #{user_id}
	and DATE(visit_date) = CURDATE();
    </select>

    <select id="selectTodayVisitCount" resultType="int">
        SELECT COUNT(*) FROM bc_club_visit
        where club_id = #{club_id}
        and DATE(visit_date) = CURDATE();
    </select>
    
    
    <!-- 소모임 어제의 방문자 수 집계 (중복 포함) -->
    <select id="yesterdayVisitCount" resultType="int">
        <![CDATA[
        SELECT COUNT(*)
        FROM bc_club_visit
        WHERE club_id = #{club_id}
        AND visit_date >= CURDATE() - INTERVAL 1 DAY
        AND visit_date < CURDATE()
        ]]>
    </select>

        <!-- 소모임 오늘의 방문자 수 집계 (중복 포함) -->
    <select id="todayVisitCount" resultType="int">
        <![CDATA[
        SELECT COUNT(*)
        FROM bc_club_visit
        WHERE club_id = #{club_id}
        AND visit_date >= CURDATE()
        AND visit_date < CURDATE() + INTERVAL 1 DAY
        ]]>
    </select>

     <select id="increasVisitCount" resultType="int">
        update bc_club_visit
        set visit_count = visit_count + 1
        where id = #{id}
    </select>

    <!-- 소모임 권한관리 -->
    <update id="roleUpdate">
        UPDATE bc_club_member
        SET role_id = #{role_id}, 
	        updated_at = NOW()
        WHERE club_id = #{club_id}
        AND user_id = #{user_id}
    </update>

    <!-- 정모 디데이 계산하는 쿼리 -->
    <select id="calculateDdaysForMeetings" resultType="String">
    <![CDATA[
        SELECT
            CASE 
	    WHEN DATEDIFF(meeting_date, CURDATE()) > 0 THEN CONCAT('D-', DATEDIFF(meeting_date, CURDATE()))
	    WHEN DATEDIFF(meeting_date, CURDATE()) = 0 THEN 'D-Day'
            ELSE CONCAT('D+', ABS(DATEDIFF(meeting_date, CURDATE())))
        END AS d_day
	    FROM 
		    bc_club_meeting
	    WHERE id = #{meeting_id}
        ]]>
    </select>

    <!-- 소모임 카테고리 이름 -->
    <select id="selectClubCategoryName" resultType="String">
        select category_name from bc_club_category
        where id = #{id}
    </select>
</mapper>