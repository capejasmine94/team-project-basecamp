<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bulmeong.basecamp.secondHandProduct.mapper.LocationSqlMapper">

    <!-- 내위치 등록    -->
    <insert id="insertMyLocation">
        INSERT INTO bc_secondhand_location
            (user_id, my_region_x, my_region_y)
        values
            (#{user_id}, #{my_region_x}, #{my_region_y})
    </insert>
    <!-- 내위치 업데이트    -->
    <update id="updateMyLocation">
        UPDATE bc_secondhand_location
        SET
            my_region_x = #{my_region_x},
            my_region_y = #{my_region_y}
        WHERE user_id = #{user_id};
    </update>
    <!-- 위칭 등록 확인 -->
    <select id="isLocationRegistered" resultType="int">
        SELECT COUNT(*) FROM bc_secondhand_location WHERE user_id = #{user_id}
    </select>
    <!-- 내 위치 가져오기 -->
    <select id="selectMyLocation" resultType="com.bulmeong.basecamp.secondHandProduct.dto.LocationDto">
        SELECT * FROM bc_secondhand_location WHERE user_id = #{user_id}
    </select>
</mapper>
